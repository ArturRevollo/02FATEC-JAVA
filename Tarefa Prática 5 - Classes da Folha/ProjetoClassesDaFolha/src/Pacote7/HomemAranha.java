package Pacote7;

public class HomemAranha {
    public boolean herói;
    public boolean uniforme;
    public boolean sentidoAranha;
    public boolean escalar;
    public boolean força;
    public boolean teias;

    public void SalvarPessoas() {
        if (herói && uniforme && sentidoAranha && escalar && força && teias) {
            System.out.println("Salvando pessoas do perigo.");

        }
    }

    public void CombaterMal() {
        if (herói && uniforme && sentidoAranha && escalar && força && teias) {
            System.out.println("Combatendo o mal.");

        }
    }

    public void Escalar() {
        if (herói && uniforme && sentidoAranha && escalar && força && teias) {
            System.out.println("Escalando paredes.");

        }
    }

    public void JogarTeias() {
        if (herói && uniforme && sentidoAranha && escalar && força && teias) {
            System.out.println("Jogando teias em possíveis alvos.");

        }
    }
}