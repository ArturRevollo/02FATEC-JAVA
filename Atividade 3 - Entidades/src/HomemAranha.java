public class HomemAranha {
    boolean herói;
    boolean uniforme;
    boolean sentidoAranha;
    boolean escalar;
    boolean força;
    boolean teias;

    void SalvarPessoas() {
        if (herói && uniforme && sentidoAranha && escalar && força && teias) {
            System.out.println("Salvando pessoas do perigo.");

        }
    }

    void CombaterMal() {
        if (herói && uniforme && sentidoAranha && escalar && força && teias) {
            System.out.println("Combatendo o mal.");

        }
    }

    void Escalar() {
        if (herói && uniforme && sentidoAranha && escalar && força && teias) {
            System.out.println("Escalando paredes.");

        }
    }

    void JogarTeias() {
        if (herói && uniforme && sentidoAranha && escalar && força && teias) {
            System.out.println("Jogando teias em alvos.");

        }
    }
}